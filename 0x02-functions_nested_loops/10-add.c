#include "main.h"
#include <stdio.h>

/**
 * add -prints add two numbers
 *@i: variable i with int value
 *@k: variable k wiht int value
 * Return: returns i + k.
 */

int add(int i, int k)
{
return (i + k);
}
