#include "main.h"

/**
 * _islower - initial short description, takes c
 * @c: integer parameter and use as condition
 * Return: returns 0.
 */

int _islower(int c)

{

if (c > 'a' && c < 'z')
{
return (1);
}

else
{
return (0);
}

}
